<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="13dp" /> <!-- 设置圆角半径，可以根据需要调整 -->
    <solid android:color="@android:color/transparent" /> <!-- 设置背景颜色 -->
    <stroke
        android:width="1dp"
    android:color="#000000" />
</shape>
